spring.application.name=xmldemo
# Server Configuration
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/xmlmanager
spring.datasource.username=postgres
spring.datasource.password=Ary282322
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Logging Configuration
logging.level.org.springframework.web=INFO
logging.level.com.example.xmlmanager=DEBUG

# Maximum file size for uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# AWS Cognito Configuration
aws.cognito.region=us-east-1
aws.cognito.userPoolId=us-east-1_wc88avkny
aws.cognito.clientId=lfqu8edjkppsrl7chspdui4le

# Spring Security OAuth2 Configuration
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=https://cognito-idp.${AWS_REGION}.amazonaws.com/${USER_POOL_ID}/.well-known/jwks.json

# CORS Configuration (adjust the allowed origins as needed)
spring.web.cors.allowed-origins=http://localhost:5173
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=Authorization,Content-Type
spring.web.cors.allow-credentials=true
spring.web.cors.max-age=3600

